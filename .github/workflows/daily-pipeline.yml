name: Daily Pipeline Sync

on:
  schedule:
    # Run daily at 07:00 UTC (16:00 JST)
    - cron: "3 7 * * *"
  workflow_dispatch:
    inputs:
      mode:
        description: "Crawl mode (incremental or full)"
        default: "incremental"
        required: true

jobs:
  sync-pipeline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"

      - run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run full pipeline (crawl, process, translate, generate)
        env:
          LLM_PROVIDER: ${{ vars.LLM_PROVIDER || 'ollama' }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_MODEL: ${{ vars.LLM_MODEL || 'gemma4:31b' }}
          CRAWL_MODE: ${{ github.event.inputs.mode || 'incremental' }}
        run: npx tsx scripts/sync-all.ts

      - name: Run leaderboard sync
        if: always()
        env:
          LLM_PROVIDER: ${{ vars.LLM_PROVIDER || 'ollama' }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_MODEL: ${{ vars.LLM_MODEL || 'gemma4:31b' }}
        run: npx tsx scripts/sync-leaderboard.ts

      - name: Run pricing sync
        if: always()
        env:
          LLM_PROVIDER: ${{ vars.LLM_PROVIDER || 'ollama' }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_MODEL: ${{ vars.LLM_MODEL || 'gemma4:31b' }}
        run: npx tsx scripts/sync-pricing.ts

      - name: Run blog generation
        if: always()
        env:
          LLM_PROVIDER: ${{ vars.LLM_PROVIDER || 'ollama' }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_MODEL: ${{ vars.LLM_MODEL || 'gemma4:31b' }}
        run: npx tsx scripts/generate-blog.ts

      - name: Commit and push changes
        run: |
          if [ -n "$(git status --porcelain src/data/ src/content/ data/)" ]; then
            git config user.name "aimodelnavi-bot"
            git config user.email "bot@aimodelnavi.jp"
            git add src/data/ src/content/ data/
            git commit -m "auto: daily pipeline sync $(date +%Y-%m-%d)"
            git push
          else
            echo "No changes to commit"
          fi